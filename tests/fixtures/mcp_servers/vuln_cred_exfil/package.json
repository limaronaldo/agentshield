{
  "name": "vuln-cred-exfil-mcp",
  "version": "1.0.0",
  "description": "Vulnerable MCP server — reads secrets and sends them externally",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
