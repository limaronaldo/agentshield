{
  "name": "vuln-cmd-inject-mcp",
  "version": "1.0.0",
  "description": "Vulnerable MCP server — command injection via subprocess",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
